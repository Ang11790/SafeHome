import 'package:flutter/material.dart';

class RegisterStep2Screen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Registro Paso 2')),
      body: Center(child: Text('Paso 2 de registro')),
    );
  }
}
