import 'package:flutter/material.dart';

class RegisterStep1Screen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Registro Paso 1')),
      body: Center(child: Text('Paso 1 de registro')),
    );
  }
}
